const welcomeTemplate = (name) => {
    return `
    <div>
      <h2>Welcome ${name} 🎉</h2>
      <p>Your account has been created successfully.</p>
    </div>
  `;
};

export default {
    welcomeTemplate,
};
